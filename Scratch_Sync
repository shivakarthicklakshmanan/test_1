from navigator import list_dashboard_tabs, _detect_chrome_labels
raw_tabs = list_dashboard_tabs(b)
print(f"{len(raw_tabs)} raw link(s) found:")
for t in raw_tabs:
    print(f"  text={t['text']!r:40s} href={t.get('href')!r}")
